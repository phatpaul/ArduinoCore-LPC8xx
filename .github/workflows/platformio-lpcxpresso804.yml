name: platformio / lpcxpresso804

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/_platformio.yml
    with:
      platform: lpcxpresso804
