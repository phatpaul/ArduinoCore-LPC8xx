name: arduino-cli / lpcxpresso804

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/_arduino-cli.yml
    with:
      platform: lpcxpresso804
